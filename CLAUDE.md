# CLAUDE.md

<!-- DAECHISECRET_KB_START -->
## 대치동시크릿 공통 지식베이스

공식 장기 지식은 비공개 저장소 `daechisecret/daechisecret-knowledge-base`에서 관리한다.

### 작업 시작

1. `DAECHISECRET_KB_PATH`가 설정되어 있으면 해당 경로를 사용한다.
2. 없으면 형제 경로 `../daechisecret-knowledge-base`를 확인한다.
3. 로컬에 없고 GitHub MCP 연결 권한이 있으면 공식 저장소에서 읽는다.
4. 먼저 `00_HOME/AI가 반드시 읽어야 할 문서.md`를 읽고 현재 작업에 해당하는 문서를 확인한다.
5. 지식베이스에 접근하지 못하면 추측하지 말고 최종 보고에 미접속 사실을 남긴다.

### 작업 종료

- 다른 작업에도 재사용할 사실, 규칙, 결정, 승인 사례, 오류 방지 원칙이 생겼는지 확인한다.
- 기존 문서를 먼저 검색하고 중복 문서를 만들지 않는다.
- AI가 새로 정리한 내용은 `draft`, 브랜드·가격·상품·교육·정책 변경은 `review`로 기록한다.
- 사용자가 명시적으로 승인한 내용만 `approved`로 처리한다.
- 비밀번호, API 키, 토큰, 고객 개인정보는 기록하지 않는다.
- 지식베이스 반영과 현재 프로젝트 코드 변경은 별도 커밋으로 구분한다.
- 이 저장소는 공개 저장소이다. 비공개 지식베이스의 내용, 경로 구조, 내부 결정, 고객정보를 이 저장소에 복사하지 않는다.
<!-- DAECHISECRET_KB_END -->
